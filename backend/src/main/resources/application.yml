server:
  port: 8081

logging:
  level:
    root: debug

spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    username: admin
    password: Ti172ne34pod
    url: jdbc:postgresql://localhost:5432/alfa
